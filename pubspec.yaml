name: your_project_name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5 # เพิ่มบรรทัดนี้ถ้าต้องการใช้ http package
  image_picker: ^0.8.4+3
  url_launcher: ^6.0.20


flutter:
  uses-material-design: true
  assets:
    - assets/line_logo.png